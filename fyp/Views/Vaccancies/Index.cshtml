@model IEnumerable<fyp.Models.Vaccancy>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VaccancyName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PostedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Category)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VaccancyName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PostedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category.ID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VaccancyID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VaccancyID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VaccancyID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
